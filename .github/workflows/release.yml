name: Build and Release

on:
  workflow_dispatch:
  push:
    branches:
      - master

permissions:
  contents: write

jobs:
  prepare:
    runs-on: ubuntu-latest
    outputs:
      tag: ${{ steps.meta.outputs.tag }}
      release_name: ${{ steps.meta.outputs.release_name }}
    steps:
      - name: Generate release metadata
        id: meta
        run: |
          tag="$(date -u +'%Y%m%d%H%M%S')"
          echo "tag=${tag}" >> "$GITHUB_OUTPUT"
          echo "release_name=${tag}" >> "$GITHUB_OUTPUT"

  build:
    needs: prepare
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            rid: win-x64
          - os: windows-latest
            rid: win-arm64
          - os: ubuntu-latest
            rid: linux-x64
          - os: ubuntu-latest
            rid: linux-arm64
          - os: macos-latest
            rid: osx-x64
          - os: macos-latest
            rid: osx-arm64

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 9.0.x

      - name: Publish (framework-dependent)
        shell: pwsh
        run: |
          dotnet publish ./NextBotInstaller/NextBotInstaller.csproj -c Release -r ${{ matrix.rid }} --self-contained false -p:UseAppHost=true -o ./out/${{ matrix.rid }}/publish

      - name: Package zip (NextBot/NextBotInstaller)
        shell: pwsh
        run: |
          $rid = "${{ matrix.rid }}"
          $publishDir = Join-Path $PWD "out/$rid/publish"
          $packageRoot = Join-Path $PWD "out/$rid/package/NextBot/NextBotInstaller"
          New-Item -ItemType Directory -Force -Path $packageRoot | Out-Null
          Copy-Item -Path (Join-Path $publishDir "*") -Destination $packageRoot -Recurse -Force

          $zipPath = Join-Path $PWD "out/$rid/NextBotInstaller-$rid.zip"
          if (Test-Path $zipPath) {
            Remove-Item $zipPath -Force
          }
          Compress-Archive -Path (Join-Path $PWD "out/$rid/package/NextBot") -DestinationPath $zipPath

      - name: Upload package
        uses: actions/upload-artifact@v4
        with:
          name: package-${{ matrix.rid }}
          path: ./out/${{ matrix.rid }}/NextBotInstaller-${{ matrix.rid }}.zip
          if-no-files-found: error

  release:
    needs:
      - prepare
      - build
    runs-on: ubuntu-latest
    steps:
      - name: Download all packages
        uses: actions/download-artifact@v4
        with:
          path: ./release-assets
          pattern: package-*
          merge-multiple: true

      - name: Create GitHub Release
        uses: ncipollo/release-action@v1
        with:
          tag: ${{ needs.prepare.outputs.tag }}
          name: ${{ needs.prepare.outputs.release_name }}
          makeLatest: true
          generateReleaseNotes: true
          artifacts: ./release-assets/*.zip
          artifactErrorsFailBuild: true
